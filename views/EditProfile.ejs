<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Profile - FarmConnect</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
    <!-- Font Awesome for the eye icon -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
</head>

<body class="bg-gray-100">
    <div class="container mx-auto min-h-screen flex items-center justify-center">
        <div class="bg-white p-8 rounded shadow-lg w-full max-w-4xl">
            <h2 class="text-2xl font-bold text-center mb-8">
                Edit Your Profile - FarmConnect
            </h2>

            <!-- Form starts here -->
            <form method="post" action="/<%= user._id %>?_method=PUT" class="space-y-4">
                <!-- Flexbox layout for two-column form -->
                <div class="flex flex-wrap -mx-4">
                    <!-- Left Column -->
                    <div class="w-full md:w-1/2 px-4 mb-4">
                        <div class="mb-4">
                            <label for="name" class="block text-gray-700">Name</label>
                            <input type="text" id="name" name="user[name]" value="<%= user.name %>"
                                class="w-full p-3 border rounded" />
                        </div>

                        <div class="mb-4">
                            <label for="email" class="block text-gray-700">Email</label>
                            <input type="email" id="email" name="user[email]" value="<%= user.email %>"
                                class="w-full p-3 border rounded" />
                        </div>

                        <div class="mb-4">
                            <label for="role" class="block text-gray-700 font-semibold">Role</label>
                            <select id="role" name="user[role]" value="<%= user.role %>" class="w-full p-3 border rounded">
                                <option value="farmer">Farmer</option>
                                <option value="buyer">Buyer</option>
                            </select>
                        </div>

                    </div>

                    <!-- Right Column -->
                    <div class="w-full md:w-1/2 px-4 mb-4">
                        <div class="mb-4">
                            <label for="number" class="block text-gray-700">Number</label>
                            <input type="text" id="number" name="user[number]" value="<%= user.number %>"
                                class="w-full p-3 border rounded" placeholder="Enter number" />
                        </div>

                        <div class="mb-4">
                            <label for="state" class="block text-gray-700">State</label>
                            <input type="text" id="state" name="user[state]" value="<%= user.state %>"
                                class="w-full p-3 border rounded" placeholder="Enter state" />
                        </div>

                        <div class="mb-4">
                            <label for="city" class="block text-gray-700">City</label>
                            <input type="text" id="city" name="user[city]" value="<%= user.city %>"
                                class="w-full p-3 border rounded" placeholder="Enter city" />
                        </div>

                        <div class="mb-4">
                            <label for="pinCode" class="block text-gray-700">Pin Code</label>
                            <input type="text" id="pinCode" name="user[pinCode]" value="<%= user.pinCode %>"
                                class="w-full p-3 border rounded" placeholder="Enter pin code" />
                        </div>
                    </div>
                </div>

                <!-- Submit Button -->
                <div class="flex justify-center">
                    <button class="bg-green-600 text-white py-3 px-6 rounded hover:bg-green-700">
                        Edit
                    </button>
                </div>
            </form>
        </div>
    </div>
<!-- 
    <script>
        function togglePasswordVisibility() {
            var passwordField = document.getElementById("password");
            var eyeIcon = document.getElementById("eyeIcon");
            if (passwordField.type === "password") {
                passwordField.type = "text";
                eyeIcon.classList.remove("fa-eye");
                eyeIcon.classList.add("fa-eye-slash");
            } else {
                passwordField.type = "password";
                eyeIcon.classList.remove("fa-eye-slash");
                eyeIcon.classList.add("fa-eye");
            }
        }
    </script> -->
</body>

</html>